import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.franschhoeklocal.app",
  appName: "Franschhoek Local",
  webDir: "dist",
  backgroundColor: "#faf7f2",
};

export default config;
